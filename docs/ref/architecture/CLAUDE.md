# Folder Guide

Architecture diagram fragments for Toss in-app runtime.

- Keep each diagram file small and task-focused.
- Keep `Source-of-Truth` paths explicit per file.
- Update `Last-Verified` when source files change.
