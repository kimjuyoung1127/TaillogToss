이 문서는 `toss-login-token-ops` 스킬 폴더 규칙이다.

## 목적
- OAuth2 토큰 발급/갱신/연동해제 운영 절차를 표준화한다.

## 파일 규칙
- 핵심 지침은 `SKILL.md` 1개에 유지한다.
- 실행 이력은 프로젝트 `docs/`로 이동한다.

## 유지보수
- 로그인 API 정책 변경 시 토큰 만료/오류코드 체크리스트를 먼저 갱신한다.
